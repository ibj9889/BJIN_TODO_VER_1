# Tasks — {{USER_NAME}} — 인덱스

> 최종 갱신: {{SETUP_DATE}}
> **이 파일은 인덱스다.** 항목별 상세 이력은 `tasks/` 폴더의 주제별 파일에 있다. "오늘 할일이 뭐야" 같은 빠른 확인은 이 인덱스만 읽어도 되고, 특정 건을 깊이 파려면 해당 주제 파일을 읽는다.
>
> 아직 주제 파일이 없다 — 실제 업무가 쌓이면 `todo-log` 스킬이 주제별로 `tasks/*.md` 파일을 자동으로 만든다.

## Active

_(아직 없음 — 업무를 기록하면 여기 쌓인다)_

## Waiting On

_(현재 없음)_

## Someday

_(현재 없음)_

## Done

완료 항목은 각 `tasks/*.md` 파일의 "## Done" 섹션 참고.

---

## 미확인 항목 (전체 인덱스)

각 주제 파일에 "이 주제 관련" 미확인 항목이 있다. 아래는 어느 주제에도 딱 맞지 않거나 여러 주제에 걸친 것만 모은 것.

| 항목 | 내용 |
|---|---|
